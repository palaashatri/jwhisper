# jwhisper

Tiny Java Swing app for local Whisper ONNX audio-to-text.

## Run

```sh
gradle run
```

Models are downloaded into `~/.jwhisper/models`. Transcription runs locally and requires `ffmpeg` on `PATH`.

## Build

```sh
gradle build
```
